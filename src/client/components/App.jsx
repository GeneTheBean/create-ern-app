import React, { Component } from 'react';

export default class App extends Component {
  render() {
    return (
      <div>
      Express, React/Redux, Node with React-Router
      </div>
    );
  }
}
